# ゲーム設計・仕様書：無限マスターへの道

## 1. ゲーム概要
「無限マスターへの道」は、ランダムに表示される絵文字の中から正解を選択し、無限の連勝を目指すカジュアルなブラウザゲームです。動的な難易度変化とランキング機能を備えています。

## 2. 基本ルール
- **多肢選択クイズ**: 各ラウンドで2個〜6個の絵文字ボタンが表示されます。
- **動的難易度**: 各ラウンドでボタンの総数と、その中の正解の数がランダムに決まります。
- **正解判定**: サーバー側で正解のインデックスが管理されます。正解が複数ある場合、いずれか一つを選択すれば次へ進めます。
- **勝利条件**: 終わりはありません。一回でも多く連勝することを目指します。
- **敗北条件**: 不正解を選択すると連勝カウントがリセットされ、ゲームオーバーとなります。

## 3. システムアーキテクチャ
### フロントエンド (HTML/CSS/JavaScript)
- **画面構成**:
    - スタート画面: ゲーム開始。
    - プレイ画面: 動的に生成される絵文字ボタン。
    - ゲームオーバー画面: 最終スコアとランキング識別トークンの表示。
    - ランキングセクション: トップ10のスコアを表示。
- **アニメーション**:
    - ボタン出現時のポップイン効果。
    - 正解時の強化された画面フラッシュ（緑・内側への影）。
    - 不正解時の滑らかなシェイク。

### バックエンド (Node.js/Express)
- **動的生成ロジック**: 毎回ランダムな絵文字と正解インデックスを生成。
- **ランキングシステム**: スコアをメモリ内に保持し、上位10名を管理。
- **トークン発行**: トップ10に入った場合、識別用のトークン（`TOP-ID-SCORE`）を発行。

## 4. 特殊な技術仕様
### 4.1 ポップアップ検証システム
- 回答時に `/check` エンドポイントを別ウィンドウで開き、サーバー側で判定。
- 正解時は `postMessage` でメイン画面に次の絵文字セットと正解数を送り、自身を閉じます。
- 不正解時はスポンサー広告へリダイレクトします。

### 4.2 ランキング識別トークン
- ゲームオーバー時、そのスコアがランキング圏内の場合、個別のIDを含むトークンを表示します。これはプレイヤーの識別や記録用として利用可能です。

## 5. 技術スタック
- **Frontend**: Vanilla HTML, CSS, JavaScript
- **Backend**: Node.js, Express
- **Dependencies**: `cors`, `uuid`
- **Infrastructure**: ポート3000、`localStorage` によるセッションID保持。
