# ゲーム設計・仕様書：目指せ十連続マスター

## 1. ゲーム概要
「目指せ十連続マスター」は、二択のクイズに連続で10回正解することを目指すカジュアルなブラウザゲームです。不正解時にはスポンサー広告が表示される収益化モデルを組み込んでいます。

## 2. 基本ルール
- **二択クイズ**: 各ラウンドで「A」または「B」のいずれかを選択します。
- **正解判定**: 正解はサーバー側でランダムに生成・管理されます。
- **勝利条件**: 連続で10回正解すると「十連続マスター達成」となり、リザルト画面が表示されます。
- **敗北条件**: 1回でも不正解になると連勝カウントがリセットされ、ゲームオーバーとなります。

## 3. システムアーキテクチャ
### フロントエンド (HTML/CSS/JavaScript)
- **画面構成**:
    - スタート画面: ゲーム説明と環境チェック実施。
    - プレイ画面: 現在の連勝数表示と二択ボタン。
    - ゲームオーバー画面: 再挑戦を促す画面。
    - 勝利画面: 達成を祝うリザルト画面。
- **アニメーション**:
    - 画面遷移時のフェードイン。
    - 正解時の画面フラッシュ（緑）。
    - 不正解時の画面シェイク（揺れ）。
    - ボタンのホバー・クリック時のスケーリング。

### バックエンド (Node.js/Express)
- **セッション管理**: UUIDを使用して個別のセッションを識別。
- **正解管理**: クライアントに正解を持たせず、サーバー側でのみ保持。
- **セッションクリーンアップ**: 1時間以上活動がないセッションを自動削除するメモリ管理機能。

## 4. 特殊な技術仕様（広告と不正防止）
### 4.1 ポップアップ検証システム
回答の検証は以下のステップで行われます：
1. プレイヤーが選択肢をクリックすると、新しいタブ（ポップアップ）で `/check` エンドポイントが開かれます。
2. サーバーが正誤を判定します。
3. **正解の場合**: ポップアップがメイン画面に通知（postMessage）を送り、自身を閉じます。
4. **不正解の場合**: ポップアップがそのままスポンサー広告（`https://otieu.com/4/10530383`）へリダイレクトされます。

### 4.2 ポップアップブロック回避（フォールバック）
- ユーザーのブラウザでポップアップがブロックされている場合、現在のウィンドウ自体を `/check` エンドポイントにリダイレクトします。
- これにより、広告表示を回避できない「絶対表示」の仕組みを実現しています。
- セッションIDは `localStorage` に保存されており、リダイレクトから戻った際も継続してプレイ可能です。

### 4.3 環境チェック (Environment Check)
ゲーム開始前に以下の2項目をチェックします：
- **広告ブロック (AdBlock)**: ダミーの広告要素（bait）が非表示・削除されていないか確認。
- **ポップアップブロック**: テスト用の `window.open` が成功するか確認。
- ブロックが検出された場合、警告を表示しますが、プレイの続行は可能です（ブロック時はリダイレクト方式で対応）。

## 5. 技術スタック
- **Frontend**: Vanilla HTML, CSS, JavaScript
- **Backend**: Node.js, Express
- **Dependencies**: `cors`, `uuid`
- **Infrastructure**: ポート3000で動作、静的ファイルを `public/` から配信。

---
本ドキュメントは、最終実装された「目指せ十連続マスター」の全仕様をまとめたものです。
